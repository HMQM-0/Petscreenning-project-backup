import { SxProps } from "@mui/material";

export const notAuthenticatedWrapper: SxProps = {
  width: "100%",
  height: "100%",
  display: "flex",
  flexDirection: "column",
  alignItems: "center",
  justifyContent: "center",
};

export const notAuthenticatedHeader: SxProps = {
  fontSize: {
    xs: "20px",
    sm: "6rem",
    padding: 0,
  },
};
